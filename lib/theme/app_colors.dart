import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand colors (serious, earthy)
  static const Color primary = Color(0xFF1B5E20); // deep organic green
  static const Color primaryVariant = Color(0xFF2E7D32);
  static const Color accent = Color(0xFF66BB6A);

  // Background and surfaces
  static const Color background =
      Color(0xFFF5F8F4); // soft off-white with a green tint
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF0D2F12);
  static const Color textSecondary = Color(0xFF4A6350);

  // Status colors
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF57F17);
  static const Color danger = Color(0xFFC62828);

  // Muted
  static const Color muted = Color(0xFF9E9E9E);
}
